<%= render 'refinery/admin/search_header', :url => refinery.blogs_admin_blogs_path %>
<div class='pagination_container'>
  <% if @blogs.any? %>
    <%= render 'blogs' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>

<main class="main-content cont" data-style="display: flex;">
  <section class="whowe">
    <h1>Other NC Catch`s Recipes</h1>
	  <ul id="recipeы">
	    <% @recipes.each do |recipe| %>
	      <li>
	      	<%= image_tag recipe.image.url, width: "100px" unless recipe.image.blank? %>
	        <%= link_to truncate( recipe.title, length: 100, escape: false), refinery.recipes_recipe_path(recipe),class: "recipes-figure-link" %>
	      </li>
	    <% end %>
	  </ul>

	</section>
	<%= render partial: "refinery/pages/events" %>
</main>

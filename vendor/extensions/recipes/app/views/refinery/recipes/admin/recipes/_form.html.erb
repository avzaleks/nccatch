<%= form_for [refinery, :recipes_admin, @recipe] do |f| -%>
  <%= render '/refinery/admin/error_messages',
              :object => @recipe,
              :include_object_name => true %>

  <div class='field'>
    <%= f.label :title -%>
    <%= f.text_field :title, :class => 'larger widest' -%>
  </div>

  <div class='field'>
    <%= f.label :image -%>
    <%= render '/refinery/admin/image_picker',
               :f => f,
               :field => :image_id,
               :image => @recipe.image,
               :toggle_image_display => false -%>
  </div>

  <div class='field'>
    <%= render '/refinery/admin/wysiwyg',
                :f => f,
                :fields => [:body],
                :object => "recipes/recipe" -%>
  </div>

  <div class='field'>
    <%= f.label :recipe_of_the_week -%>
    <%= f.check_box :recipe_of_the_week, :checked => @recipe[:recipe_of_the_week] -%>
  </div>

  <%= render '/refinery/admin/form_actions', :f => f,
             :continue_editing => false,
             :delete_title => t('delete', :scope => 'refinery.recipes.admin.recipes.recipe'),
             :delete_confirmation => t('message', :scope => 'refinery.admin.delete', :title => @recipe.title) -%>
<% end -%>

<% content_for :javascripts do -%>
  <script>
    $(document).ready(function(){
      page_options.init(false, '', '');
    });
  </script>
<% end -%>
